syntax = "proto3";

message GetWeatherRequest {
  string city = 1;
}

message Weather {
  string city = 1;
  string weather = 2;
}

service WeatherService {
  rpc weather(GetWeatherRequest) returns (Weather);
}